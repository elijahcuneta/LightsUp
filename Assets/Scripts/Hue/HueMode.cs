using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HueMode : MonoBehaviour {

    public enum HuePick { Normal, Hidden, Move };
    public HuePick huePicked;
}
